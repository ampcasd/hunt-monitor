<Window x:Class="TibiaSquare.HuntMonitor.Infrastructure.ReportDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Send Diagnostic Report"
        Width="500" Height="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">
    <StackPanel Margin="20">
        <TextBlock Text="Describe the issue you experienced:"
                   Margin="0,0,0,8" FontWeight="SemiBold"/>
        <TextBox x:Name="DescriptionBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 Height="150"
                 VerticalScrollBarVisibility="Auto"
                 FontSize="14"
                 Padding="4"/>
        <TextBlock Margin="0,8,0,16"
                   TextWrapping="Wrap"
                   Foreground="Gray"
                   FontSize="12"
                   Text="The report includes logs from your last 2 play sessions, OCR diagnostics, and system info. No personal data beyond your Discord account is included."/>

        <TextBlock x:Name="ErrorText"
                   Foreground="Red"
                   FontSize="12"
                   Margin="0,0,0,8"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Width="80"
                    Margin="0,0,8,0"
                    Click="Cancel_Click"/>
            <Button x:Name="SendButton"
                    Content="Send Report"
                    Width="100"
                    IsDefault="True"
                    Click="Send_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
