# Processing DLL

`TibiaSquare.HuntMonitor.Processing.dll` lives here so local builds of the public desktop app are functional.

Microsoft Store packages include the same processing binary. The public source repository does not publish unsigned GitHub release builds.

The DLL is distributed as an obfuscated binary. Its public interface is defined in `src/TibiaSquare.HuntMonitor/Private/`.
